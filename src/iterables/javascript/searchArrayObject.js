searchArrayObject (array, search) {
    array.forEach(function (obj) {
      for (var key in obj) {
        var name = el[key]
        if (name === seach) {
          return el.role
        }
      }
    })
  }
