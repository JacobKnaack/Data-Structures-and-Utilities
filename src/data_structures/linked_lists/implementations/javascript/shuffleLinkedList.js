// function that takes in a linked list and shuffles their values
module.exports = function shuffleLinkedList(list) {
  var length = 0;

  for ( var key in list ) {
    

    if (key === null) {
      return list
    }
  }
} 
